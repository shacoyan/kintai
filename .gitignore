.vercel

# Build output
dist/
.env*.local

# Playwright E2E
playwright-report/
test-results/
e2e/.auth/*.json

# 視覚回帰: tmp/ は ad-hoc 保存先として維持（L26 互換 / 手動撮影用）
tmp/

# 視覚回帰 baseline は commit 対象（L29 以降）。
# e2e/visual-regression.spec.ts-snapshots/ は意図的に gitignore しない。
